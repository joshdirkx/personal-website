import React from 'react';

interface MiddlePaneProps {
    splitType: ['vertical', 'horizontal'];
}
function MiddlePane({ splitType }: MiddlePaneProps) {
    return (
        <div>
        </div>
    );
}

export default MiddlePane;
